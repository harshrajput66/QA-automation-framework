[pytest]
testpaths = tests
markers =
    ui: UI tests using Playwright
    api: API tests using requests
    data: Data/database validation tests
    smoke: Quick smoke tests
    regression: Full regression tests
    e2e: End-to-end tests

addopts =
    -v
    --tb=short
    --strict-markers
